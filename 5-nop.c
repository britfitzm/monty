#include "monty.h"

bus_t bus;

/**
* b_nop - function that does nothing
* @head: double head pointer to stack
* @counter: line count
* Return: nothing
*/

void b_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
